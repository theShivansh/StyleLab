"""Signing and caller identity.

Small on purpose. Everything here is replaced by real authentication in S11; what must not
change then is the property it already holds — the caller cannot choose which user's
wardrobe they are looking at.
"""
